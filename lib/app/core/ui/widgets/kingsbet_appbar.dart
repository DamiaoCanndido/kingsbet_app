import 'package:flutter/material.dart';

class KingsbetAppBar extends AppBar {
  KingsbetAppBar({
    super.key,
    super.title,
    super.elevation = 2,
    super.backgroundColor,
    super.centerTitle = true,
    super.iconTheme = const IconThemeData(color: Colors.white),
    super.bottom,
  });
}
